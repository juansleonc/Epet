# Core
Django>=3.2,<4.0
djangorestframework>=3.12.0
# django-cors-headers~=3.7.0
# django-environ~=0.4.5
# djangorestframework-simplejwt~=4.6.0

# Deploy
# gunicorn~=20.0.4
# dj-database-url~=0.5.0

# Development
# django-debug-toolbar~=3.2
# python-dotenv~=0.15.0

# Security
# Consider using pip-audit to check for vulnerabilities in these packages

# Utilities (add based on your project's needs)
# Pillow~=8.1.1  # If you're dealing with image processing
# celery~=5.0.5  # For asynchronous task queue

# Note:
# 1. The `~=` operator is used for versions to allow backward-compatible updates.
# 2. Regularly audit your dependencies for security vulnerabilities.
# 3. Consider separating production and development dependencies into separate files for larger projects.
